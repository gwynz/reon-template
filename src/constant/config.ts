import { origin_url } from "@/constant/env";

export const siteConfig = {
	title: "Reon",
	description: "Find review and compare products",
	/** Without additional '/' on the end, e.g. https://gwyn.info */
	url: origin_url,
};
